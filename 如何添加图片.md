# 如何添加背景图和头像

## 步骤 1: 准备图片

将你的图片放到 `frontend/src/assets/` 目录下：
- 背景图（例如：background.jpg）
- 头像（例如：avatar.jpg）

## 步骤 2: 添加背景图

编辑 `frontend/src/components/WelcomePage.jsx` 文件：

1. 找到第 2 行，取消注释并修改为你的背景图文件名：
```javascript
import backgroundImg from '../assets/background.jpg'; // 改成你的文件名
```

2. 找到第 6 行，修改为：
```javascript
<div className="welcome-page" style={{ backgroundImage: `url(${backgroundImg})` }}>
```

或者直接编辑 `frontend/src/components/WelcomePage.css` 文件：

找到第 7 行，注释掉渐变背景，取消注释背景图：
```css
/* background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); */
background-image: url('../assets/background.jpg'); /* 改成你的文件名 */
```

## 步骤 3: 添加头像

编辑 `frontend/src/components/MainPage.jsx` 文件：

1. 找到第 3 行，取消注释并修改为你的头像文件名：
```javascript
import avatarImg from '../assets/avatar.jpg'; // 改成你的文件名
```

2. 找到第 103 行，取消注释：
```javascript
<img src={avatarImg} alt="布布" className="avatar" />
```

## 步骤 4: 保存并刷新

保存所有文件后，浏览器会自动刷新显示新的背景图和头像。

## 支持的图片格式

- JPG/JPEG
- PNG
- GIF
- WebP
- SVG

## 建议的图片尺寸

- 背景图：1920x1080 或更大
- 头像：200x200 或更大（会自动裁剪为圆形）
